// Fixed import to use @sveltejs/kit instead of missing $types
import { json } from "@sveltejs/kit"
import type { RequestHandler } from "@sveltejs/kit"
import type { FireData, ChangeDetectionResult } from "$lib/types"
